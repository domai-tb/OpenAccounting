import 'package:drift/drift.dart';

/// Stub for rechnung triggers — original file missing in repo HEAD.
/// ponytail: no-op install keeps AppDatabase.ensureOpen green until real triggers land.
class RechnungTriggers {
  static Future<void> install(QueryExecutor executor) async {}
}
