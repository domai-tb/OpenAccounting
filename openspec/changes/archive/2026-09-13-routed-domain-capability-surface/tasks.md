## 1. Canonical route inventory — Every canonical route has a useful surface

- [x] 1.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_every_canonical_route_has_useful_surface (assert it fails for the right reason)
- [x] 1.2 Implement the behavior required by Canonical route inventory to pass 1.1
- [x] 1.3 Refactor the implementation and fixtures; the full suite stays green

## 2. Canonical route inventory — Database outage is not an empty route

- [x] 2.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_database_outage_is_not_empty_route (assert it fails for the right reason)
- [x] 2.2 Implement the behavior required by Canonical route inventory to pass 2.1
- [x] 2.3 Refactor the implementation and fixtures; the full suite stays green

## 3. Canonical route inventory — Alias matrix preserves deep links

- [x] 3.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_alias_matrix_preserves_deep_links (assert it fails for the right reason)
- [x] 3.2 Implement the behavior required by Canonical route inventory to pass 3.1
- [x] 3.3 Refactor the implementation and fixtures; the full suite stays green

## 4. Canonical route inventory — Route matrix exposes a truthful boundary

- [x] 4.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_route_matrix_exposes_truthful_boundary (assert it fails for the right reason)
- [x] 4.2 Implement the behavior required by Canonical route inventory to pass 4.1
- [x] 4.3 Refactor the implementation and fixtures; the full suite stays green

## 5. Typed domain data and actions — Invoice detail exposes lifecycle actions

- [x] 5.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_invoice_detail_exposes_lifecycle_actions (assert it fails for the right reason)
- [x] 5.2 Implement the behavior required by Typed domain data and actions to pass 5.1
- [x] 5.3 Refactor the implementation and fixtures; the full suite stays green

## 6. Typed domain data and actions — Invalid record is safely reported

- [x] 6.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_invalid_record_is_safely_reported (assert it fails for the right reason)
- [x] 6.2 Implement the behavior required by Typed domain data and actions to pass 6.1
- [x] 6.3 Refactor the implementation and fixtures; the full suite stays green

## 7. Scalable domain lists — Contact search paginates

- [x] 7.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_contact_search_paginates (assert it fails for the right reason)
- [x] 7.2 Implement the behavior required by Scalable domain lists to pass 7.1
- [x] 7.3 Refactor the implementation and fixtures; the full suite stays green

## 8. Scalable domain lists — List query failure is retryable

- [x] 8.1 Write failing test test/features/routed_surface/typed_route_test.dart → test_list_query_failure_is_retryable (assert it fails for the right reason)
- [x] 8.2 Implement the behavior required by Scalable domain lists to pass 8.1
- [x] 8.3 Refactor the implementation and fixtures; the full suite stays green

## 9. Dashboard cards lead to real capabilities — Open invoices card opens receivables

- [x] 9.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_open_invoices_card_opens_receivables (assert it fails for the right reason)
- [x] 9.2 Implement the behavior required by Dashboard cards lead to real capabilities to pass 9.1
- [x] 9.3 Refactor the implementation and fixtures; the full suite stays green

## 10. Dashboard cards lead to real capabilities — Unavailable inventory card is truthful

- [x] 10.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_unavailable_inventory_card_is_truthful (assert it fails for the right reason)
- [x] 10.2 Implement the behavior required by Dashboard cards lead to real capabilities to pass 10.1
- [x] 10.3 Refactor the implementation and fixtures; the full suite stays green

## 11. Dashboard cards lead to real capabilities — Dashboard configuration failure is recoverable

- [x] 11.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_dashboard_configuration_failure_is_recoverable (assert it fails for the right reason)
- [x] 11.2 Implement the behavior required by Dashboard cards lead to real capabilities to pass 11.1
- [x] 11.3 Refactor the implementation and fixtures; the full suite stays green

## 12. Setup exposes persisted configuration — Setup summary shows real names

- [x] 12.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_setup_summary_shows_real_names (assert it fails for the right reason)
- [x] 12.2 Implement the behavior required by Setup exposes persisted configuration to pass 12.1
- [x] 12.3 Refactor the implementation and fixtures; the full suite stays green

## 13. Setup exposes persisted configuration — Setup write failure is recoverable

- [x] 13.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_setup_write_failure_is_recoverable (assert it fails for the right reason)
- [x] 13.2 Implement the behavior required by Setup exposes persisted configuration to pass 13.1
- [x] 13.3 Refactor the implementation and fixtures; the full suite stays green

## 14. Setup exposes persisted configuration — Successful profile switch rebinds services

- [x] 14.1 Write failing test test/features/routed_surface/dashboard_setup_test.dart → test_successful_profile_switch_rebinds_services (assert it fails for the right reason)
- [x] 14.2 Implement the behavior required by Setup exposes persisted configuration to pass 14.1
- [x] 14.3 Refactor the implementation and fixtures; the full suite stays green

## 15. Bank import retry and outcome fidelity — Manual review count is shown

- [x] 15.1 Write failing test test/features/routed_surface/bank_import_test.dart → test_manual_review_count_is_shown (assert it fails for the right reason)
- [x] 15.2 Implement the behavior required by Bank import retry and outcome fidelity to pass 15.1
- [x] 15.3 Refactor the implementation and fixtures; the full suite stays green

## 16. Bank import retry and outcome fidelity — Initial data retry reloads

- [x] 16.1 Write failing test test/features/routed_surface/bank_import_test.dart → test_initial_data_retry_reloads (assert it fails for the right reason)
- [x] 16.2 Implement the behavior required by Bank import retry and outcome fidelity to pass 16.1
- [x] 16.3 Refactor the implementation and fixtures; the full suite stays green

## 17. Import history is actionable — History row opens details

- [x] 17.1 Write failing test test/features/routed_surface/bank_import_test.dart → test_history_row_opens_details (assert it fails for the right reason)
- [x] 17.2 Implement the behavior required by Import history is actionable to pass 17.1
- [x] 17.3 Refactor the implementation and fixtures; the full suite stays green

## 18. Import history is actionable — Empty history offers import

- [x] 18.1 Write failing test test/features/routed_surface/bank_import_test.dart → test_empty_history_offers_import (assert it fails for the right reason)
- [x] 18.2 Implement the behavior required by Import history is actionable to pass 18.1
- [x] 18.3 Refactor the implementation and fixtures; the full suite stays green

## 19. Import history is actionable — Status policy controls actions

- [x] 19.1 Write failing test test/features/routed_surface/bank_import_test.dart → test_status_policy_controls_actions (assert it fails for the right reason)
- [x] 19.2 Implement the behavior required by Import history is actionable to pass 19.1
- [x] 19.3 Refactor the implementation and fixtures; the full suite stays green
