## Implementation Tasks

## 1. Chart-of-accounts seeds have valid reporting mappings — Fresh profile supports reporting without custom fixtures

- [ ] 1.1 Write failing test test/integration/audit/seed-master-data-contract_test.dart → test_seed_master_data_contract_1_1_fresh_profile_supports_reporting_without_custom_fixtures (assert it fails for the right reason)
- [ ] 1.2 Implement the behavior required by Chart-of-accounts seeds have valid reporting mappings to pass 1.1
- [ ] 1.3 Refactor the implementation and fixtures; the full suite stays green

## 2. Chart-of-accounts seeds have valid reporting mappings — Seed upgrade preserves user edits

- [ ] 2.1 Write failing test test/integration/audit/seed-master-data-contract_test.dart → test_seed_master_data_contract_1_2_seed_upgrade_preserves_user_edits (assert it fails for the right reason)
- [ ] 2.2 Implement the behavior required by Chart-of-accounts seeds have valid reporting mappings to pass 2.1
- [ ] 2.3 Refactor the implementation and fixtures; the full suite stays green

## 3. Bank templates are documented and deterministic — Supported template parses its format

- [ ] 3.1 Write failing test test/integration/audit/seed-master-data-contract_test.dart → test_seed_master_data_contract_2_1_supported_template_parses_its_format (assert it fails for the right reason)
- [ ] 3.2 Implement the behavior required by Bank templates are documented and deterministic to pass 3.1
- [ ] 3.3 Refactor the implementation and fixtures; the full suite stays green

## 4. Bank templates are documented and deterministic — Unknown format is not silently mapped

- [ ] 4.1 Write failing test test/integration/audit/seed-master-data-contract_test.dart → test_seed_master_data_contract_2_2_unknown_format_is_not_silently_mapped (assert it fails for the right reason)
- [ ] 4.2 Implement the behavior required by Bank templates are documented and deterministic to pass 4.1
- [ ] 4.3 Refactor the implementation and fixtures; the full suite stays green
