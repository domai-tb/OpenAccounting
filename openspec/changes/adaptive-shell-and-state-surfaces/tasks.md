## 1. Width-aware shell motion — Rail expands without route loss

- [x] 1.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_rail_expands_without_route_loss (assert it fails for the right reason)
- [x] 1.2 Implement the behavior required by Width-aware shell motion to pass 1.1
- [x] 1.3 Refactor the implementation and fixtures; the full suite stays green

## 2. Width-aware shell motion — Breakpoint edges preserve route and filters

- [x] 2.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_breakpoint_edges_preserve_route_and_filters (assert it fails for the right reason)
- [x] 2.2 Implement the behavior required by Width-aware shell motion to pass 2.1
- [x] 2.3 Refactor the implementation and fixtures; the full suite stays green

## 3. Width-aware shell motion — Reduced motion bypasses animation

- [x] 3.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_reduced_motion_bypasses_animation (assert it fails for the right reason)
- [x] 3.2 Implement the behavior required by Width-aware shell motion to pass 3.1
- [x] 3.3 Refactor the implementation and fixtures; the full suite stays green

## 4. Stable persisted navigation lifecycle — Persisted compact state is first-frame stable

- [x] 4.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_persisted_compact_state_is_first_frame_stable (assert it fails for the right reason)
- [x] 4.2 Implement the behavior required by Stable persisted navigation lifecycle to pass 4.1
- [x] 4.3 Refactor the implementation and fixtures; the full suite stays green

## 5. Stable persisted navigation lifecycle — Invalid preference uses safe default

- [x] 5.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_invalid_preference_uses_safe_default (assert it fails for the right reason)
- [x] 5.2 Implement the behavior required by Stable persisted navigation lifecycle to pass 5.1
- [x] 5.3 Refactor the implementation and fixtures; the full suite stays green

## 6. Stable persisted navigation lifecycle — Drawer navigation closes and preserves focus

- [x] 6.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_drawer_navigation_closes_and_preserves_focus (assert it fails for the right reason)
- [x] 6.2 Implement the behavior required by Stable persisted navigation lifecycle to pass 6.1
- [x] 6.3 Refactor the implementation and fixtures; the full suite stays green

## 7. Stable persisted navigation lifecycle — Preference write failure is recoverable

- [x] 7.1 Write failing test test/features/shell/adaptive_shell_test.dart → test_preference_write_failure_is_recoverable (assert it fails for the right reason)
- [x] 7.2 Implement the behavior required by Stable persisted navigation lifecycle to pass 7.1
- [x] 7.3 Refactor the implementation and fixtures; the full suite stays green

## 8. Content-shaped loading previews — Dashboard card shows skeleton content

- [x] 8.1 Write failing test test/features/shell/loading_state_test.dart → test_dashboard_card_shows_skeleton_content (assert it fails for the right reason)
- [x] 8.2 Implement the behavior required by Content-shaped loading previews to pass 8.1
- [x] 8.3 Refactor the implementation and fixtures; the full suite stays green

## 9. Content-shaped loading previews — Skeleton resolves to an error state

- [x] 9.1 Write failing test test/features/shell/loading_state_test.dart → test_skeleton_resolves_to_error_state (assert it fails for the right reason)
- [x] 9.2 Implement the behavior required by Content-shaped loading previews to pass 9.1
- [x] 9.3 Refactor the implementation and fixtures; the full suite stays green

## 10. Content-shaped loading previews — Routed page keeps title and controls while loading

- [x] 10.1 Write failing test test/features/shell/loading_state_test.dart → test_routed_page_keeps_title_and_controls_while_loading (assert it fails for the right reason)
- [x] 10.2 Implement the behavior required by Content-shaped loading previews to pass 10.1
- [x] 10.3 Refactor the implementation and fixtures; the full suite stays green

## 11. State transition identity — Empty list offers a primary action

- [x] 11.1 Write failing test test/features/shell/loading_state_test.dart → test_empty_list_offers_primary_action (assert it fails for the right reason)
- [x] 11.2 Implement the behavior required by State transition identity to pass 11.1
- [x] 11.3 Refactor the implementation and fixtures; the full suite stays green

## 12. State transition identity — Failed request preserves context

- [x] 12.1 Write failing test test/features/shell/loading_state_test.dart → test_failed_request_preserves_context (assert it fails for the right reason)
- [x] 12.2 Implement the behavior required by State transition identity to pass 12.1
- [x] 12.3 Refactor the implementation and fixtures; the full suite stays green

## 13. State transition identity — Failed mutation rolls back safely

- [x] 13.1 Write failing test test/features/shell/loading_state_test.dart → test_failed_mutation_rolls_back_safely (assert it fails for the right reason)
- [x] 13.2 Implement the behavior required by State transition identity to pass 13.1
- [x] 13.3 Refactor the implementation and fixtures; the full suite stays green

## 14. Locale-complete visible UI — English state copy is complete

- [x] 14.1 Write failing test test/features/shell/localized_test.dart → test_english_state_copy_is_complete (assert it fails for the right reason)
- [x] 14.2 Implement the behavior required by Locale-complete visible UI to pass 14.1
- [x] 14.3 Refactor the implementation and fixtures; the full suite stays green

## 15. Locale-complete visible UI — Missing translation fails validation

- [x] 15.1 Write failing test test/features/shell/localized_test.dart → test_missing_translation_fails_validation (assert it fails for the right reason)
- [x] 15.2 Implement the behavior required by Locale-complete visible UI to pass 15.1
- [x] 15.3 Refactor the implementation and fixtures; the full suite stays green

## 16. Locale-complete visible UI — Locale formats accounting values

- [x] 16.1 Write failing test test/features/shell/localized_test.dart → test_locale_formats_accounting_values (assert it fails for the right reason)
- [x] 16.2 Implement the behavior required by Locale-complete visible UI to pass 16.1
- [x] 16.3 Refactor the implementation and fixtures; the full suite stays green

## 17. Accessible keyboard and semantics contract — Focused navigation activates

- [x] 17.1 Write failing test test/features/shell/accessible_test.dart → test_focused_navigation_activates (assert it fails for the right reason)
- [x] 17.2 Implement the behavior required by Accessible keyboard and semantics contract to pass 17.1
- [x] 17.3 Refactor the implementation and fixtures; the full suite stays green

## 18. Accessible keyboard and semantics contract — Narrow action remains reachable

- [x] 18.1 Write failing test test/features/shell/accessible_test.dart → test_narrow_action_remains_reachable (assert it fails for the right reason)
- [x] 18.2 Implement the behavior required by Accessible keyboard and semantics contract to pass 18.1
- [x] 18.3 Refactor the implementation and fixtures; the full suite stays green

## 19. Accessible keyboard and semantics contract — Focus semantics survive state settling

- [x] 19.1 Write failing test test/features/shell/accessible_test.dart → test_focus_semantics_survive_state_settling (assert it fails for the right reason)
- [x] 19.2 Implement the behavior required by Accessible keyboard and semantics contract to pass 19.1
- [x] 19.3 Refactor the implementation and fixtures; the full suite stays green
