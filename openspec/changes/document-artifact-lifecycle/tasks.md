## 1. Complete supported PDF rendering — Supported type renders bytes

- [ ] 1.1 Write failing test test/features/pdf/pdf_rendering_test.dart → test_supported_type_renders_bytes (assert it fails for the right reason)
- [ ] 1.2 Implement the behavior required by Complete supported PDF rendering to pass 1.1
- [ ] 1.3 Refactor the implementation and fixtures; the full suite stays green

## 2. Complete supported PDF rendering — Unsupported or incomplete snapshot fails

- [ ] 2.1 Write failing test test/features/pdf/pdf_rendering_test.dart → test_unsupported_or_incomplete_snapshot_fails (assert it fails for the right reason)
- [ ] 2.2 Implement the behavior required by Complete supported PDF rendering to pass 2.1
- [ ] 2.3 Refactor the implementation and fixtures; the full suite stays green

## 3. Optional content and readable layout — Configured content appears

- [ ] 3.1 Write failing test test/features/pdf/pdf_rendering_test.dart → test_configured_content_appears (assert it fails for the right reason)
- [ ] 3.2 Implement the behavior required by Optional content and readable layout to pass 3.1
- [ ] 3.3 Refactor the implementation and fixtures; the full suite stays green

## 4. Optional content and readable layout — Missing optional content remains readable

- [ ] 4.1 Write failing test test/features/pdf/pdf_rendering_test.dart → test_missing_optional_content_remains_readable (assert it fails for the right reason)
- [ ] 4.2 Implement the behavior required by Optional content and readable layout to pass 4.1
- [ ] 4.3 Refactor the implementation and fixtures; the full suite stays green

## 5. Atomic artifact and side-effect transaction — Finalized invoice commits artifact and effects

- [ ] 5.1 Write failing test test/features/pdf/document_transaction_test.dart → test_finalized_invoice_commits_artifact_and_effects (assert it fails for the right reason)
- [ ] 5.2 Implement the behavior required by Atomic artifact and side-effect transaction to pass 5.1
- [ ] 5.3 Refactor the implementation and fixtures; the full suite stays green

## 6. Atomic artifact and side-effect transaction — Writer failure rolls back

- [ ] 6.1 Write failing test test/features/pdf/document_transaction_test.dart → test_writer_failure_rolls_back (assert it fails for the right reason)
- [ ] 6.2 Implement the behavior required by Atomic artifact and side-effect transaction to pass 6.1
- [ ] 6.3 Refactor the implementation and fixtures; the full suite stays green

## 7. Retry, concurrency, and path safety — Identical retry is idempotent

- [ ] 7.1 Write failing test test/features/pdf/document_transaction_test.dart → test_identical_retry_is_idempotent (assert it fails for the right reason)
- [ ] 7.2 Implement the behavior required by Retry, concurrency, and path safety to pass 7.1
- [ ] 7.3 Refactor the implementation and fixtures; the full suite stays green

## 8. Retry, concurrency, and path safety — Concurrent collision is rejected

- [ ] 8.1 Write failing test test/features/pdf/document_transaction_test.dart → test_concurrent_collision_is_rejected (assert it fails for the right reason)
- [ ] 8.2 Implement the behavior required by Retry, concurrency, and path safety to pass 8.1
- [ ] 8.3 Refactor the implementation and fixtures; the full suite stays green

## 9. Routed artifact actions — User previews and saves an artifact

- [ ] 9.1 Write failing test test/features/pdf/document_actions_test.dart → test_user_previews_and_saves_artifact (assert it fails for the right reason)
- [ ] 9.2 Implement the behavior required by Routed artifact actions to pass 9.1
- [ ] 9.3 Refactor the implementation and fixtures; the full suite stays green

## 10. Routed artifact actions — Viewer reports unsupported print

- [ ] 10.1 Write failing test test/features/pdf/document_actions_test.dart → test_viewer_reports_unsupported_print (assert it fails for the right reason)
- [ ] 10.2 Implement the behavior required by Routed artifact actions to pass 10.1
- [ ] 10.3 Refactor the implementation and fixtures; the full suite stays green

## 11. Missing artifact recovery — Missing file offers regeneration

- [ ] 11.1 Write failing test test/features/pdf/document_actions_test.dart → test_missing_file_offers_regeneration (assert it fails for the right reason)
- [ ] 11.2 Implement the behavior required by Missing artifact recovery to pass 11.1
- [ ] 11.3 Refactor the implementation and fixtures; the full suite stays green

## 12. Missing artifact recovery — Unsafe path is rejected

- [ ] 12.1 Write failing test test/features/pdf/document_actions_test.dart → test_unsafe_path_is_rejected (assert it fails for the right reason)
- [ ] 12.2 Implement the behavior required by Missing artifact recovery to pass 12.1
- [ ] 12.3 Refactor the implementation and fixtures; the full suite stays green
