## Implementation Tasks

## 1. Maintained specifications are parseable under strict Anvil validation — All maintained specs pass

- [ ] 1.1 Run openspec validate --all --strict; confirm this scenario's structural/conflict check passes

## 2. Maintained specifications are parseable under strict Anvil validation — Malformed spec blocks a release

- [ ] 2.1 Run openspec validate --all --strict; confirm this scenario's structural/conflict check passes

## 3. Conflicting normative requirements are reconciled — Duplicate behavior has one owner

- [ ] 3.1 Run openspec validate --all --strict; confirm this scenario's structural/conflict check passes

## 4. Conflicting normative requirements are reconciled — Unresolved conflict is visible

- [ ] 4.1 Run openspec validate --all --strict; confirm this scenario's structural/conflict check passes
